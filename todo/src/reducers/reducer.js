import React from 'react'

const initialTodos = {
  item: 'Learn about reducers',
  completed: false,
  id: 389298759
}

const todoReducer = (state, action) => {
  switch(action.type) {
    case('ADD_TODO'):
      if {

      }

  }
}